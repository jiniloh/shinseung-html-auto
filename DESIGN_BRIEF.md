# 디자인 의뢰서 — 주일 성경 본문 HTML 생성기 (도구 UI 전면 개편)

> **의뢰 대상:** 클로드 디자인
> **개편 범위:** 도구 자체의 작업 화면 UI (`index.html` + `index.css`)
> **개편 제외:** 생성되는 게시판용 결과 HTML (`index.js` 내 인라인 스타일) — 이번 의뢰 범위 아님
> **작성일:** 2026-05-31
> **현재 버전:** v2.3

---

## 1. 한 줄 요약

교회 주일 예배용 성경 본문을 입력하면 게시판에 붙여넣을 HTML을 생성해 주는 **1페이지 웹 도구**의 작업 화면을, 더 깔끔하고 쓰기 좋게 **전면 리디자인**해 주세요. 기능·동작·DOM 구조(특히 element ID)는 그대로 유지하고, **시각 디자인과 레이아웃 감각만** 새로 입혀 주세요.

---

## 2. 이 도구는 무엇인가

- **사용자:** 교회 사무/봉사자(비개발자). 매주 1회 정도, PC 브라우저에서 사용.
- **하는 일:**
  1. 말씀 MP3 파일명, 패밀리모임지 링크, 성경 본문을 입력하고
  2. 성경 선택창(성경/장/시작 절/끝 절)으로 개역개정 본문을 자동으로 끌어와 붙이거나 직접 붙여넣은 뒤
  3. **「HTML 생성 + 미리보기」** 버튼으로 게시판용 HTML을 만들고
  4. **「결과 HTML 복사」** 로 복사해 교회 게시판에 붙여넣음.
- **성격:** 회원가입·로그인 없는 정적 단일 페이지. 프레임워크 없이 순수 HTML/CSS/JS.

---

## 3. 현재 화면 구조 (그대로 유지)

좌우 2단 레이아웃(데스크톱 기준). 좁은 화면에선 1단으로 스택됩니다.

```
┌───────────────────────── app-card ─────────────────────────┐
│ 헤더: 제목 + 설명                                  v2.3      │
│                                                             │
│ ┌──── 입력(좌) ────┐   ┌──────── 결과(우) ────────┐        │
│ │ 말씀 MP3 파일명   │   │ 생성된 HTML  [결과복사]    │       │
│ │ 패밀리모임지 링크 │   │ (output textarea)         │        │
│ │  └ 안내 툴팁      │   │                           │        │
│ │ 성경 본문         │   │ 미리보기                   │       │
│ │  └ 선택창(성경/장/ │   │ (preview)                 │        │
│ │     시작절/끝절)  │   │                           │        │
│ │  └ [본문추가][비움]│  │                           │        │
│ │  └ textarea       │   │                           │        │
│ │  [HTML 생성+미리보기]│ │                           │        │
│ └──────────────────┘   └───────────────────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

현재 톤: 회청색 계열(`--primary: #3f6fb6`), 라운드 카드, 부드러운 그림자. 무난하지만 밋밋하고 위계가 약함.

---

## 4. 개편 목표

1. **첫인상 개선** — 비개발자도 "정돈됐다, 믿음직하다"고 느끼는 화면.
2. **위계 강화** — 입력 → 생성 → 결과의 흐름이 시각적으로 분명하게.
3. **핵심 동선 강조** — 가장 중요한 액션(「HTML 생성 + 미리보기」, 「결과 HTML 복사」)이 한눈에 들어오게.
4. **가독성** — 한글 본문/라벨이 편안하게 읽히는 타이포·여백·대비.
5. **일관성** — 버튼·입력·카드의 스타일 규칙이 통일.
6. **반응형 유지** — 데스크톱 2단 / 모바일 1단 모두 자연스럽게.

---

## 5. 디자인 방향 제안 (택1 또는 혼합)

방향은 클로드에게 위임되었습니다. 아래 3가지 시안 방향 중 하나를 고르거나 섞어 제안해 주세요. **각 방향마다 컬러 팔레트·타이포·버튼 스타일·카드 처리를 구체화**해서 보여 주시면 좋겠습니다.

### 방향 A — 모던 미니멀
- 넉넉한 여백, 가는 보더, 절제된 색(거의 무채색 + 포인트 1색).
- 평평한(flat) 카드, 또렷한 타이포 위계. 그림자 최소화.
- "도구답고 군더더기 없는" 느낌.

### 방향 B — 따뜻한 교회 감성
- 부드러운 크림/베이지 배경 + 차분한 포인트색(딥블루/세이지/버건디 등).
- 둥근 모서리, 은은한 그림자, 약간의 장식 요소(구분선·아이콘).
- 신앙 커뮤니티에 어울리는 포근하고 신뢰감 있는 톤.

### 방향 C — 차분한 프로페셔널 (현재 톤 고급화)
- 지금의 회청색 계열을 유지하되, 팔레트를 정제하고 간격·정렬·그림자를 다듬어 완성도를 끌어올림.
- 가장 보수적·안전한 선택. 기존 사용자 위화감 최소.

> 제안 시 **각 방향의 색상 토큰(hex), 폰트 스택, 버튼 4종 스타일, 입력·카드 처리**를 명시해 주세요.

---

## 6. 반드시 유지해야 할 것 (기술 제약 — 매우 중요)

`index.js`가 DOM을 **ID로 직접 참조**합니다. 마크업을 새로 짜더라도 **아래 element ID는 같은 의미로 반드시 존재**해야 합니다. (태그·클래스·배치는 바꿔도 됩니다.)

| ID | 역할 | 요소 종류 |
|---|---|---|
| `mp3` | 말씀 MP3 파일명 입력 | `<input>` |
| `lesson` | 패밀리모임지 링크 입력 | `<input>` |
| `lessonNotice` | 링크 자동변환 안내 메시지 | 텍스트 컨테이너 |
| `bible` | 성경 본문 textarea | `<textarea>` |
| `bookSelect` / `chapterSelect` / `verseStartSelect` / `verseEndSelect` | 성경/장/시작절/끝절 선택 | `<select>` |
| `addPassageBtn` | 본문 추가 버튼 | `<button>` |
| `clearPassageBtn` | 본문 비우기 버튼 | `<button>` |
| `bibleStatus` | 선택창 상태 메시지 | 텍스트 컨테이너 |
| `generateBtn` | HTML 생성 + 미리보기 | `<button>` |
| `output` | 생성된 HTML 표시 | `<textarea>` |
| `copyBtn` | 결과 HTML 복사 | `<button>` |
| `copyMsg` | 복사 완료 메시지 | 텍스트 컨테이너 |
| `preview` | 미리보기 영역 | 컨테이너(`<div>`) |
| `tooltipWrap` / `infoBtn` / `helpBox` | 패밀리모임지 안내 툴팁 묶음/트리거/내용 | div / button / div |

**JS가 토글하는 클래스명도 유지**해 주세요 (스타일은 새로 입혀도 됨):
- `tooltipWrap`에 `hover-active`, `helpBox`에 `active` → 툴팁 표시
- `lessonNotice`에 `field-notice`, `is-visible`, `field-notice-success`, `field-notice-warning` → 안내 메시지 색상
- 미리보기 안내문 `preview-notice` (JS가 생성)

### 기타 제약
- **순수 HTML/CSS/JS** 유지. 빌드 도구·프레임워크·외부 CSS 라이브러리 도입 금지.
- 폰트는 시스템/웹폰트 자유. 단 **외부 의존이 늘면 로딩 안정성**을 함께 고려.
- 모든 텍스트는 **한국어** 유지.
- `guide.png`(툴팁 안내 이미지)는 계속 사용.
- **접근성**: 현재의 `aria-labelledby`, `aria-live`, `<label for>` 연결을 보존하거나 더 개선.

---

## 7. 산출물

- **이번 의뢰의 산출물은 본 의뢰서 문서까지**입니다. 실제 코드 구현은 이 의뢰서를 바탕으로 별도 진행.
- 디자이너(클로드)에게 기대하는 회신:
  1. **추천 방향 1개** (5장 A/B/C 중) + 이유
  2. **디자인 토큰 명세**: 컬러(hex), 타이포(폰트·크기·굵기·행간), 간격 스케일, 라운드/그림자 규칙
  3. **컴포넌트 스타일 가이드**: 버튼 4종(primary/secondary/success/ghost), 입력·셀렉트·textarea, 카드, 툴팁, 안내 메시지
  4. **레이아웃 시안**: 데스크톱 2단 / 모바일 1단 와이어프레임 또는 설명
  5. (선택) 적용 시 주의점·리스크

---

## 8. 참고 파일

- 현재 마크업: `index.html`
- 현재 스타일: `index.css`
- DOM을 참조하는 로직: `index.js` (이 파일은 수정 대상 아님 — ID/클래스 계약의 근거)
- 툴팁 안내 이미지: `guide.png`

---

*문의/수정 요청은 이 문서에 코멘트로 남겨 주세요.*
